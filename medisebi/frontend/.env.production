# ── MediSebi Frontend Production Config ─────────────
# Set this to your deployed backend API URL when deploying frontend separately.
# Example: https://api.yourdomain.com/api/v1
# If frontend and backend are on the same domain (reverse proxy), use: /api/v1
VITE_API_BASE_URL=https://medisebi-api.onrender.com/api/v1
